@typeparam TPlanet where TPlanet : PlanetBase
@inherits PlanetView<TPlanet>

@* The fallback look: a planet that hasn't been designed yet, or doesn't need designing. *@
<circle class="planet" cx="@X" cy="@Y" r="@Radius" />
